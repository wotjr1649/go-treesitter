The incomplete C# scheduling ablation is not included as a patch in Git history.
Its raw overlay and preparation scripts remain under ignored .scratch storage;
the phase-local defer-core.diff is also ignored. Its source hashes, observed
outputs, rejected hypotheses and trace conclusions remain in this phase. This
preserves failures without committing an implementation that failed its target,
as required by ADR-0005. No diagnostic implementation reaches the product.
