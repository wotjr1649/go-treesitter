The two module rows in binary-buildinfo-01.log end with a tab emitted by
`go version -m`. The raw evidence is retained unchanged. These are the only
staged whitespace reports; source, notices, workflow and other evidence pass.
