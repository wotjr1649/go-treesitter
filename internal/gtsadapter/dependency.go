// Package gtsadapter is the sole import boundary to the pinned runtime.
package gtsadapter

// Keep the baseline in the build graph before the parser implementation lands.
import _ "github.com/odvcencio/gotreesitter"
