diff --git a/common/define-grammar.js b/common/define-grammar.js
index efb0b76..9db3d44 100644
--- a/common/define-grammar.js
+++ b/common/define-grammar.js
@@ -656,6 +656,15 @@ module.exports = function defineGrammar(dialect) {

       override_modifier: _ => 'override',

+      // TypeScript 4.7 variance annotations on type parameters.
+      // `in` marks a type parameter as contravariant, `out` as covariant,
+      // and `in out` explicitly marks it as invariant.
+      variance: _ => choice(
+        seq('in', 'out'),
+        'in',
+        'out',
+      ),
+
       required_parameter: $ => seq(
         $._parameter_name,
         field('type', optional($.type_annotation)),
@@ -771,6 +780,7 @@ module.exports = function defineGrammar(dialect) {
         $.array_type,
         $.tuple_type,
         $.flow_maybe_type,
+        $.import_type,
         $.type_query,
         $.index_type_query,
         alias($.this, $.this_type),
@@ -876,6 +886,16 @@ module.exports = function defineGrammar(dialect) {
         )),
         field('arguments', $.arguments),
       ),
+      import_type: $ => prec.right(1, seq(
+        'import',
+        '(',
+        field('argument', $.string),
+        ')',
+        optional(seq(
+          '.',
+          field('qualifier', choice($._type_identifier, $.nested_type_identifier)),
+        )),
+      )),
       _type_query_instantiation_expression: $ => seq(
         field('function', choice(
           $.import,
@@ -892,6 +912,7 @@ module.exports = function defineGrammar(dialect) {
           alias($._type_query_member_expression, $.member_expression),
           alias($._type_query_call_expression, $.call_expression),
           alias($._type_query_instantiation_expression, $.instantiation_expression),
+          $.import_type,
           $.identifier,
           $.this,
         ),
@@ -962,22 +983,44 @@ module.exports = function defineGrammar(dialect) {
         choice('{', '{|'),
         optional(seq(
           optional(choice(',', ';')),
-          sepBy1(
-            choice(',', $._semicolon),
-            choice(
-              $.export_statement,
-              $.property_signature,
-              $.call_signature,
-              $.construct_signature,
-              $.index_signature,
-              $.method_signature,
+          choice(
+            seq(
+              repeat($._terminated_object_type_member),
+              $._object_type_member,
+              optional(choice(',', $._semicolon)),
             ),
+            repeat1($._terminated_object_type_member),
           ),
-          optional(choice(',', $._semicolon)),
         )),
         choice('}', '|}'),
       ),

+      _object_type_member: $ => choice(
+        $.export_statement,
+        $.property_signature,
+        $.call_signature,
+        $.construct_signature,
+        $.index_signature,
+        $.method_signature,
+      ),
+
+      _terminated_object_type_member: $ => choice(
+        prec.right(1, seq(
+          $.call_signature,
+          choice(',', $._semicolon, $._function_signature_automatic_semicolon),
+        )),
+        prec.right(1, seq(
+          choice(
+            $.export_statement,
+            $.property_signature,
+            $.construct_signature,
+            $.index_signature,
+            $.method_signature,
+          ),
+          choice(',', $._semicolon),
+        )),
+      ),
+
       call_signature: $ => $._call_signature,

       property_signature: $ => seq(
@@ -1004,6 +1047,7 @@ module.exports = function defineGrammar(dialect) {

       type_parameter: $ => seq(
         optional('const'),
+        optional($.variance),
         field('name', $._type_identifier),
         field('constraint', optional($.constraint)),
         field('value', optional($.default_type)),
diff --git a/common/scanner.h b/common/scanner.h
index 23b78b5..03eeab2 100644
--- a/common/scanner.h
+++ b/common/scanner.h
@@ -123,7 +123,6 @@ static bool scan_automatic_semicolon(TSLexer *lexer, const bool *valid_symbols,
         case '*':
         case '%':
         case '>':
-        case '<':
         case '=':
         case '?':
         case '^':
@@ -133,6 +132,9 @@ static bool scan_automatic_semicolon(TSLexer *lexer, const bool *valid_symbols,
         case ':':
             return false;

+        case '<':
+            return valid_symbols[FUNCTION_SIGNATURE_AUTOMATIC_SEMICOLON];
+
         case '{':
             if (valid_symbols[FUNCTION_SIGNATURE_AUTOMATIC_SEMICOLON]) {
                 return false;
@@ -165,6 +167,9 @@ static bool scan_automatic_semicolon(TSLexer *lexer, const bool *valid_symbols,
             // Don't insert a semicolon before `in` or `instanceof`, but do insert one
             // before an identifier.
         case 'i':
+            if (!valid_symbols[LOGICAL_OR]) {
+                return true;
+            }
             skip(lexer);

             if (lexer->lookahead != 'n') {
